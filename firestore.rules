rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read for all dashboard data
    match /{document=**} {
      allow read: if true;
      allow write: if false;  // Write only via server (cron jobs)
    }
  }
}
